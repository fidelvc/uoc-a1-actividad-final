En la actividad final te pedimos que pienses en una actividad cotidiana, como hicimos con la receta de la tortilla, y que desarrolles un algoritmo que sirva para resolverla.

No te pedimos que hagas otra receta, sinó que te plantees una situación diferente, que se pueda resolver usando el pensamiento computacional. Usa tu imaginación.

La resolución de esta actividad requiere que entregues 2 ficheros: 

    Un fichero PDF en el que desarrolles el proceso que te hemos indicado para trabajar el pensamiento computacional para programar. El nombre del fichero deberá seguir el siguiente formato: JavaScript_A1_Nombre_Apellido.pdf
    El documento deberá contener las siguientes secciones:

        Definición del problema: explica el problema, y cómo defines la solución del mismo. Detalla las variables esenciales necesarias para resolverlo (alrededor de media página).
        Descomposición del problema: qué tareas simples forman el problema completo (de media a una página)
        Identificación de patrones: detalla las tareas que has identificado que corresponden a un patrón que puede servir para simplificar la estructura del programa final (de media a una página)
        Abstracción: describe las adaptaciones del problema necesarias para resolver el problema usando un algoritmo, es decir, cómo has transformado el problema en un programa. Por ejemplo, en el programa de la tortilla no podemos hacer una tortilla físicamente, pero le podemos decir a un cocinero los pasos que ha de seguir para hacerla (de media a una página).
        Algoritmo final: detalla las acciones que se han de seguir para resolver el problema planteado.
        Ten en cuenta que el algoritmo final tiene que contener:
                variables de tipo numéricas, de texto y booleanas
                operaciones aritméticas con algunas de esas variables
                una estructura de tipo condicional
                una estructura con contador
        Pseudocódigo: plantea un algoritmo adaptado a la programación para resolver el problema

2. Un fichero ipynb (jupyter notebook) que incluya el programa de la tortilla modificado tal y como os pedimos a continuación. 

2.1. Crea el fichero ipynb:

        Entra a la plataforma Jupyter Notebook, en la carpeta Escola de programació // JavaScript_A1 // 

        Enlaces a un sitio externo.
        Crea un nuevo fichero ipynb: arriba a la derecha clica sobre New // Javascript (Node.js)
        Cambia el nombre del fichero: arriba, a la derecha del logo de jupyterhub, clica sobre "Untitled". Escribe el nombre del fichero según el siguiente formato: JavaScript_A1_Nombre_Apellido

2.2. Copia el contenido del programa de la tortilla:

        En la plataforma Jupyter Notebook, dentro de la carpeta  Escola de programació // JavaScript_A1 // 4_7_JavaScript-Code_Implementation.ipynb
        Selecciona y copia el contenido del programa de la tortilla.
        Pega el contenido del programa en el fichero ipynb que has creado en el apartado anterior. 

2.3. Modifica el programa para que:

        El número de personas a las que se va a hacer la tortilla sea de 10.
        Añade un nuevo ingrediente "levadura" a la lista de ingredientes. Este ingrediente ha de ser opcional, como la sal, por lo tanto sólo deberá mostrarse si la variable correspondiente es cierta. 
        Después de echar las patatas al bol con huevo, modifica el programa para que le diga al cocinero que ha de añadir la levadura a la mezcla, si el valor de "levadura" es "true" (como en el caso de la sal).
        Finalmente, es preciso descargar el archivo en formato ipynb, para lo cual debes acceder a la opción "File -> Download as -> Notebook". Este archivo debe ser añadido a esta entrega, de lo contrario se calificará como: "No entregado".

¡Mucha suerte con tu primer programa!